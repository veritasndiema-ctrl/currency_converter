# Copy this file to db.properties (same folder) and fill in your own
# values. db.properties is gitignored so real credentials never get
# committed — db.properties.example is the template that does.
db.url=jdbc:mysql://localhost:3306/currency_converter?useSSL=false&serverTimezone=UTC
db.user=root
db.password=your_password_here
